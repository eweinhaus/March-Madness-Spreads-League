rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All collections are server-only (backend uses Admin SDK which bypasses rules).
    // Deny all client reads and writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
